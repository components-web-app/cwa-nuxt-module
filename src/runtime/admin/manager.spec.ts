import { describe, test } from 'vitest'

describe('Manager class tests', () => {
  describe('addToStack function', () => {
    test.todo('If the last click target does not equal the new click target, clear the stack and save the new click target', () => {})

    test.todo('Adds a stack object to the stack array', () => {})
  })
})
